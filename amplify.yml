version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p .amplify-hosting/static
        - cp index.html .amplify-hosting/static/index.html
        - cp deploy-manifest.json .amplify-hosting/deploy-manifest.json
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
